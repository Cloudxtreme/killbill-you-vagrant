killbill:
  version: 0.12.1
  webapp_path: /var/lib/tomcat7/webapps/ROOT.war
  plugins_dir: /var/tmp/bundles
  plugins:
    ruby:
      - name: stripe-plugin
        version: 0.2.1
kaui:
  version: LATEST
  webapp_path: /var/lib/tomcat7/webapps/kaui
